# 학습 > 문제집

## 경로
- 목록: `/study-sets`
- 생성: `/study-sets/new`
- 상세: `/study-sets/[id]`

## 목적
회원이 자신의 문제집을 생성하고 관리하는 페이지

## 개념
- **문제집 (Study Set)**: 학습 자료(PDF)들을 담는 컨테이너
- **학습 자료 (Study Material)**: 문제집에 추가하는 개별 PDF 파일
- **구조**: 문제집 1개 → 학습 자료 N개 (PDF)

## 주요 기능

### 1. 문제집 목록 (/study-sets)

#### 통계 카드 (3개)
- 전체 문제집 개수
- 총 문제 수
- 학습 완료 개수

#### 검색 기능
- 문제집명으로 검색

#### 문제집 카드
각 카드 표시 정보:
- 문제집명
- 문제집 개요 (2줄 말줄임)
- 학습 자료 개수
- 문제 개수
- 생성일
- 학습 상태 뱃지 (학습 중)

#### 카드 액션
- **클릭**: 문제집 상세 페이지로 이동
- **수정 버튼**: 인라인 편집 모드
  - 문제집명 수정
  - 문제집 개요 수정
  - 저장/취소 버튼
- **삭제 버튼**: 확인 메시지 후 삭제

#### 빈 상태
> "아직 문제집이 없습니다. 상단의 '새 문제집 만들기' 버튼을 눌러 문제집을 만들어보세요"

### 2. 문제집 생성 (/study-sets/new)

#### 필수 입력
- **문제집명**: 텍스트 입력 (필수)
- **문제집 개요**: 여러 줄 입력 (선택)

#### 자동 설정
- **자격증**: 사용자의 이용권에서 자동 설정
- **시험일**: 사용자의 이용권에서 자동 설정

#### 생성 프리뷰
입력 시 실시간 프리뷰:
```
📚 {문제집명}:{자격증명}_{시험일}
학습자료 0개 · 문제 0개
```

#### 이용권 필요
이용권이 없는 경우:
> "⚠️ 이용권이 필요합니다. 문제집을 만들려면 먼저 이용권을 구매해야 합니다."
> [이용권 구매하러 가기] 버튼

#### 안내 메시지
> "💡 나중에 여러 학습자료(PDF)를 이 문제집에 추가할 수 있습니다"

### 3. 문제집 상세 (/study-sets/[id])
⚠️ **미구현** - 향후 구현 예정
- 학습 자료 목록
- PDF 업로드
- 문제 생성 상태
- 학습 시작 버튼

## API 엔드포인트

### GET /study-sets
문제집 목록 조회
```json
{
  "data": [
    {
      "id": "uuid",
      "name": "정보처리기사 2024",
      "description": "필기 대비 문제집",
      "certification_id": "uuid",
      "total_materials": 3,
      "total_questions": 150,
      "created_at": "2025-01-15T10:00:00Z",
      "learning_status": "in_progress"
    }
  ],
  "total": 5
}
```

### POST /study-sets/create
문제집 생성 (FormData)
```
name: "문제집명"
description: "문제집 개요" (선택)
certification_id: "uuid"
```

**응답:**
```json
{
  "study_set": {
    "id": "uuid",
    "name": "문제집명",
    "description": "문제집 개요",
    "certification_id": "uuid"
  }
}
```

### PATCH /study-sets/{id}
문제집 수정 (TODO: 미구현)
```json
{
  "name": "수정된 문제집명",
  "description": "수정된 개요"
}
```

### DELETE /study-sets/{id}
문제집 삭제 (TODO: 미구현)

## 구현 상태
- ✅ 문제집 목록 조회
- ✅ 검색 기능
- ✅ 인라인 편집 UI (로컬 상태만)
- ✅ 삭제 UI (로컬 상태만)
- ✅ 문제집 생성 폼
- ✅ 이용권 자동 연동
- ⚠️ 수정/삭제 API (TODO 주석 처리)
- ❌ 문제집 상세 페이지

## 파일 위치
- **목록**: `/frontend/src/app/(dashboard)/study-sets/page.tsx`
- **생성**: `/frontend/src/app/(dashboard)/study-sets/new/page.tsx`
- **상세**: `/frontend/src/app/(dashboard)/study-sets/[id]/page.tsx` (미구현)
- **Backend**: `/backend/app/api/v1/endpoints/study_sets.py`
- **Repository**: `/backend/app/repositories/mock_study_set.py`

## 다음 단계
1. 문제집 수정/삭제 API 구현
2. 문제집 상세 페이지 구현
3. PDF 업로드 기능
4. 문제 생성 파이프라인 연동
