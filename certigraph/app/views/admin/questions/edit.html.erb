<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900">문제 수정</h1>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <%= render 'form', question: @question %>
  </div>

  <div class="mt-6 flex gap-3">
    <%= link_to '취소', admin_question_path(@question), class: 'btn btn-secondary' %>
    <%= link_to '목록으로', admin_questions_path, class: 'btn btn-outline' %>
  </div>
</div>
