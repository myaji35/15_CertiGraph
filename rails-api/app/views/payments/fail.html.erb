<div class="min-h-screen bg-gray-50 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full">
    <div class="bg-white rounded-lg shadow-lg p-8 text-center">
      <!-- Error Icon -->
      <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-red-100 mb-4">
        <svg class="h-10 w-10 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </div>

      <h2 class="text-2xl font-bold text-gray-900 mb-2">결제 실패</h2>
      <p class="text-gray-600 mb-6">결제 처리 중 문제가 발생했습니다.</p>

      <div class="bg-red-50 rounded-lg p-4 mb-6 text-left">
        <div class="space-y-2">
          <% if @error_code %>
          <div class="flex justify-between">
            <span class="text-gray-600">오류 코드</span>
            <span class="font-medium text-red-600"><%= @error_code %></span>
          </div>
          <% end %>
          <% if @error_message %>
          <div>
            <span class="text-gray-600 block mb-1">오류 메시지</span>
            <span class="text-sm text-red-600"><%= @error_message %></span>
          </div>
          <% end %>
          <% if @order_id %>
          <div class="flex justify-between">
            <span class="text-gray-600">주문번호</span>
            <span class="font-medium"><%= @order_id %></span>
          </div>
          <% end %>
        </div>
      </div>

      <div class="space-y-3">
        <a href="/payments/checkout" class="block w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transition">
          다시 시도하기
        </a>
        <a href="/dashboard" class="block w-full bg-gray-200 text-gray-700 py-3 rounded-lg hover:bg-gray-300 transition">
          대시보드로 돌아가기
        </a>
      </div>

      <div class="mt-6 bg-blue-50 rounded-lg p-4 text-left">
        <h3 class="font-medium text-blue-900 mb-2">도움이 필요하신가요?</h3>
        <ul class="text-sm text-blue-800 space-y-1">
          <li>• 카드 정보를 다시 확인해주세요</li>
          <li>• 결제 한도를 확인해주세요</li>
          <li>• 다른 결제 수단을 시도해보세요</li>
          <li>• 문제가 계속되면 고객센터로 문의해주세요</li>
        </ul>
      </div>

      <p class="mt-6 text-sm text-gray-500">
        고객센터: support@certigraph.com
      </p>
    </div>
  </div>
</div>
