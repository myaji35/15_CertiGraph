# 🚀 CertiGraph 병렬 테스트 실행 가이드

## 📊 테스트 그룹 구성

### 병렬 실행 그룹 (동시 실행 가능)

#### Group 1: 인증 테스트 (auth-parallel)
- **Worker 수**: 8개
- **예상 시간**: 5-8분
- **테스트 파일**: `tests/e2e/parallel/auth*.spec.ts`
- **특징**: 각 테스트가 독립적인 사용자 계정 사용

#### Group 2: 읽기 전용 테스트 (read-only-parallel)
- **Worker 수**: 8개
- **예상 시간**: 3-5분
- **테스트 파일**: `tests/e2e/parallel/readonly*.spec.ts`
- **특징**: 데이터 변경 없음, 완전 병렬 가능

#### Group 3: 독립적인 E2E 테스트 (independent-e2e)
- **Worker 수**: 8개
- **예상 시간**: 10-15분
- **테스트 파일**: 
  - `bmad-knowledge-graph.spec.ts`
  - `bmad-performance.spec.ts`
  - `bmad-security.spec.ts`
- **특징**: 서로 영향을 주지 않는 독립적인 기능 테스트

### 순차 실행 그룹 (순서대로 실행)

#### Group 4: 결제 플로우 (payment-sequential)
- **Worker 수**: 1개
- **예상 시간**: 10-15분
- **테스트 파일**: `tests/e2e/payment/*.spec.ts`
- **이유**: Toss Payments API 제한, 상태 의존성

#### Group 5: 통합 테스트 (integration-sequential)
- **Worker 수**: 1개
- **예상 시간**: 15-20분
- **테스트 파일**: 
  - `bmad-integration.spec.ts`
  - `bmad-full-test.spec.ts`
- **이유**: 전체 플로우 테스트, 데이터 의존성

#### Group 6: 학습 자료 및 시험 (study-exam-partial)
- **Worker 수**: 2개
- **예상 시간**: 12-18분
- **테스트 파일**: 
  - `bmad-study-materials.spec.ts`
  - `bmad-mock-exam.spec.ts`
- **이유**: 부분적 병렬 가능 (사용자별 격리)

#### Group 7: 인증 포괄 테스트 (auth-comprehensive)
- **Worker 수**: 2개
- **예상 시간**: 8-12분
- **테스트 파일**: 
  - `bmad-auth-comprehensive.spec.ts`
  - `bmad-auth-social-password.spec.ts`
- **이유**: 복잡한 인증 플로우, 부분적 의존성

## 🎯 실행 명령어

### 1. 빠른 병렬 테스트 (개발 중)
```bash
npm run test:parallel
```
- 병렬 가능한 테스트만 실행
- 예상 시간: 10-15분
- 빠른 피드백 제공

### 2. 순차 테스트 (통합 검증)
```bash
npm run test:sequential
```
- 순차 실행이 필요한 테스트만 실행
- 예상 시간: 25-35분
- 전체 플로우 검증

### 3. 전체 테스트 실행
```bash
npm run test:all
```
- 모든 테스트 그룹 실행
- 예상 시간: 40-60분 (병렬화로 최적화)
- CI/CD 파이프라인용

### 4. 특정 그룹 테스트

#### 인증 테스트만
```bash
npm run test:auth
```

#### 결제 테스트만
```bash
npm run test:payment
```

#### 통합 테스트만
```bash
npm run test:integration
```

#### 학습/시험 테스트만
```bash
npm run test:study-exam
```

### 5. 디버깅 모드
```bash
npm run test:debug
```
- 단계별 실행
- 브라우저 표시
- 중단점 설정 가능

### 6. UI 모드 (추천)
```bash
npm run test:ui
```
- 시각적 테스트 실행
- 실시간 결과 확인
- 테스트 선택 실행

### 7. CI/CD 모드
```bash
npm run test:ci
```
- 헤드리스 모드
- 재시도 2회
- Worker 4개

## 📈 테스트 결과 확인

### HTML 리포트 보기
```bash
npm run test:report
```

### 결과 파일 위치
- HTML: `test-results/html-report/index.html`
- JSON: `test-results/results.json`
- JUnit: `test-results/junit.xml`

## 🔧 병렬화 전략

### 충돌 방지 메커니즘

#### 1. 사용자 격리
각 테스트는 고유한 사용자 계정 사용:
```typescript
const testUsers = {
  worker0: 'test-w0@certigraph.test',
  worker1: 'test-w1@certigraph.test',
  worker2: 'test-w2@certigraph.test',
  // ... worker7까지
};
```

#### 2. 데이터베이스 격리
- 읽기 전용 테스트: 공유 데이터 사용
- 쓰기 테스트: 사용자별 격리
- 트랜잭션 롤백 활용

#### 3. 파일 시스템 격리
```
/tmp/test-uploads/
  ├── w0/  (Worker 0)
  ├── w1/  (Worker 1)
  └── w2/  (Worker 2)
```

#### 4. 세션 격리
- 각 테스트는 독립적인 브라우저 컨텍스트
- 쿠키/로컬스토리지 자동 정리
- 테스트 간 상태 공유 없음

## 🐛 버그 수정 워크플로우

### 1단계: 테스트 실행
```bash
npm run test:all
```

### 2단계: 실패한 테스트 확인
```bash
npm run test:report
```
- HTML 리포트에서 실패 원인 분석
- 스크린샷, 비디오, 트레이스 확인

### 3단계: 버그 수정
- 실패한 테스트의 에러 메시지 확인
- 코드 수정

### 4단계: 재테스트
```bash
# 실패한 테스트만 재실행
npx playwright test --last-failed

# 또는 특정 테스트만
npx playwright test tests/e2e/bmad-auth-comprehensive.spec.ts
```

### 5단계: 전체 회귀 테스트
```bash
npm run test:all
```

## 📊 예상 실행 시간

### 로컬 환경 (8 workers)
- 병렬 테스트: 10-15분
- 순차 테스트: 25-35분
- **전체 테스트: 40-60분**

### CI/CD 환경 (4 workers)
- 병렬 테스트: 15-20분
- 순차 테스트: 30-40분
- **전체 테스트: 50-70분**

### 순차 실행 (비교)
- **전체 테스트: 180-240분 (3-4시간)**

### 성능 개선
- **시간 절감: 약 70-75%**
- **병렬화 효율: 85%**

## 🎯 테스트 커버리지 목표

### 현재 상태
- E2E 테스트: 320개 시나리오
- 단위 테스트: 추가 예정
- 통합 테스트: 추가 예정

### 목표
- E2E 커버리지: 60% 이상
- 통합 테스트: 70% 이상
- 단위 테스트: 80% 이상
- **전체 커버리지: 75% 이상**

## 🔄 반복 테스트 전략

### Flaky 테스트 감지
```bash
# 동일 테스트 10회 반복
npx playwright test --repeat-each=10 tests/e2e/bmad-payment.spec.ts
```

### 부하 테스트
```bash
# 모든 worker 동시 실행
npx playwright test --workers=12
```

### 야간 전체 테스트
```bash
# 모든 브라우저에서 실행
npx playwright test --project=chromium --project=firefox --project=webkit
```

## 📝 베스트 프랙티스

### 1. 테스트 격리
- ✅ 각 테스트는 독립적으로 실행 가능
- ✅ 테스트 간 상태 공유 금지
- ✅ beforeEach/afterEach로 정리

### 2. 안정적인 선택자
- ✅ data-testid 사용
- ❌ CSS 클래스명 사용 지양
- ✅ 의미있는 ARIA 레이블

### 3. 명확한 대기
- ✅ waitForSelector 사용
- ❌ 고정 시간 대기 (sleep) 지양
- ✅ 네트워크 요청 완료 대기

### 4. 에러 처리
- ✅ 명확한 에러 메시지
- ✅ 스크린샷 자동 캡처
- ✅ 재시도 로직 (flaky 테스트 대비)

## 🚨 문제 해결

### 테스트가 타임아웃되는 경우
```typescript
// playwright.config.ts에서 타임아웃 증가
timeout: 120 * 1000, // 2분
```

### Worker 간 충돌 발생
```bash
# Worker 수 줄이기
npx playwright test --workers=4
```

### 메모리 부족
```bash
# 순차 실행으로 전환
npx playwright test --workers=1
```

### 데이터베이스 충돌
- 각 테스트가 고유한 사용자 사용하는지 확인
- 트랜잭션 롤백 확인
- 테스트 후 정리 코드 확인

## 📞 지원

문제 발생 시:
1. HTML 리포트 확인
2. 트레이스 파일 분석
3. 스크린샷/비디오 검토
4. GitHub Issues에 보고
